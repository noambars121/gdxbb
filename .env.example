# GA4 measurement ID (e.g. G-XXXXXXXXXX).
# Leave unset to disable analytics entirely — no scripts are injected without it.
# Set it here for local dev and in Vercel project env settings for production,
# then rebuild/redeploy. No code changes are required.
NEXT_PUBLIC_GA_MEASUREMENT_ID=
